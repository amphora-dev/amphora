# 任务 <代号>：<一句话目标>（<仓库>）

工作目录：`<相对仓库根的路径，如 ../proton-wine>`（分支 `<branch>`，HEAD 应为 `<sha>`，不符就 FAIL）
允许修改：`<文件或目录白名单>`
禁止：git commit / push / stash / checkout / reset；修改白名单外文件；联网写操作。
（发布类步骤改为列出允许的精确命令，并禁止 `--force`、推 main、取消 CI、手改 manifest。）

## 背景（已由主代理核实，不要重新调查）

- <事实 1，附证据：文件:行号 / 日志原文 / 提交 SHA>
- <事实 2>
- 项目规则：<与本任务相关的 AGENTS.md 约束>

## 要做的改动 / 步骤

1. <具体到文件、函数、宏名；给出期望写法>
2. <拿不准的保留，并在交付里列出>

## 自检（必须执行并贴出输出）

```
<grep 期望无输出的命令>
<计数期望等于 N 的命令>
git diff --stat
```

## 最终输出

自检结果、完整 diff（或关键证据）、偏离说明（一句话）。
最后一行必须是：
`<KEY>_RESULT=OK <关键字段>` 或 `<KEY>_RESULT=FAIL reason=<一句话>`
