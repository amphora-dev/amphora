<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: mid-session IME inject relay. Not merged into release. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
            Exported so adb can target it. Same-UID startActivity reaches
            non-exported WineAndroidSessionActivity (singleTask → onNewIntent).
            MainActivity mid-session am start only brings the task forward.
        -->
        <activity
            android:name=".gamesession.wineandroid.WineAndroidDebugImeRelayActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
    </application>
</manifest>
